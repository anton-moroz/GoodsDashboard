<div class="w-full sm:max-w-md mt-6 px-6 py-4 bg-white shadow-md overflow-hidden sm:rounded-lg">
    {{ form_start(goodForm) }}
    {{ form_row(goodForm.name) }}
    {{ form_row(goodForm.price) }}
    {{ form_row(goodForm.currency) }}
    {{ form_row(goodForm.quantity) }}

    <input type="hidden" name="_csrf_token" value="{{ csrf_token('good') }}">

    <div class="flex items-center justify-end mt-4">
        {{ include('components/common/btn-submit.html.twig', { transKey: buttonTextKey }) }}
    </div>

    {{ form_end(goodForm) }}
</div>
